{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "lots": {
      ".indexOn": ["currentHolderId", "growerId"],
      ".read": "auth != null && ((query.orderByChild === 'growerId' && query.equalTo === auth.uid) || (query.orderByChild === 'currentHolderId' && query.equalTo === auth.uid))",
      "$lotId": {
        ".read": "auth != null",
        ".write": "auth != null && ((data.exists() && data.child('growerId').val() === auth.uid) || (data.exists() && data.child('currentHolderId').val() === auth.uid) || (newData.exists() && newData.child('growerId').val() === auth.uid) || (newData.exists() && newData.child('currentHolderId').val() === auth.uid))"
      }
    },

    "stations": {
      ".indexOn": ["retailerId"],
      ".read": "auth != null && query.orderByChild === 'retailerId' && query.equalTo === auth.uid",
      "$stationId": {
        ".read": "auth != null && data.child('retailerId').val() === auth.uid",
        ".write": "auth != null && ((data.exists() && data.child('retailerId').val() === auth.uid) || (!data.exists() && newData.child('retailerId').val() === auth.uid))",
        "retailerId": {
          ".validate": "!data.exists() || newData.val() === data.val()"
        },
        "temp": {
          ".validate": "newData.isNumber() && newData.val() >= -10 && newData.val() <= 60"
        },
        "humid": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
        }
      }
    },

    "alerts": {
      ".indexOn": ["retailerId", "growerId"],
      ".read": "auth != null && ((query.orderByChild === 'retailerId' && query.equalTo === auth.uid) || (query.orderByChild === 'growerId' && query.equalTo === auth.uid))",
      "$alertId": {
        ".read": "auth != null && (data.child('retailerId').val() === auth.uid || data.child('growerId').val() === auth.uid)",
        ".write": "auth != null && ((data.exists() && data.child('retailerId').val() === auth.uid) || (newData.exists() && newData.child('retailerId').val() === auth.uid))"
      }
    },

    "config": {
      ".read": "auth != null",
      ".write": false
    }
  }
}
